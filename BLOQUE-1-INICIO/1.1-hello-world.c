#include <stdio.h>  // Libreria que permite ejecutar ciertas funciones, como printf
// #include <stdlib.h>  // libreria que permite ejecutar system(), sin embargo, es posible que no sea necesario
// incluir la libreria, ya que en .c, ya viene incluido


int main() {
    printf("Hello World!"); // Es una funcion incluida en la libreria stdio
    system("pause");
    return 0;
}